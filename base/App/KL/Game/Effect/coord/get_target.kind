App.KL.Game.Effect.coord.get_target: App.KL.Game.Effect<Hexagonal.Axial>
  (center, target, map)
  let target = List.head!(target) <> center

  App.KL.Game.Effect.Result.new!(target, center, map, Hexagonal.Axial.Map.new!, 0, [])
